{erl_opts,
 [ debug_info
 ]}.

{deps, [
       ]}.

{pre_hooks,
 [ {"(linux|darwin|solaris|win32)", compile, "bash -c 'cd .. && make'"}
 , {"(linux|darwin|solaris|win32)", clean, "cd .. && make clean"}
 ]}.
